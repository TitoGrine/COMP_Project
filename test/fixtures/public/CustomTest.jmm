import static ioPlus.printHelloWorld(void) void;
import static ioPlus.print();
import ioPlus;
import ioPlus.plus(int, int, boolean);
import Test;
import Stonk;

class CustomTest extends Test{
    int something;
    Stonk stonk;
    boolean truth;

	public int stonk(int[] args, int nut) {
        int num;
        boolean star;

        args[5] + 2;
        num = 5;
        star = false;
        truth = !star;

        //if((num < 1 && num < 1)){
        if((truth && !star) < !num && (2-3/45 < 34+5) < 4/4*1){
            num = 5;
        } else {
            num = 7;
        }

        while(!num){
            star = true;
            num = !star * 3+2;
        }

        if((2*3-4) < (num+4/2)){
            ioPlus.plus(2, 3, 2);
            arg[3] && 4;
        } else {
            args[5];
            num = 7*3;
        }

        a = 8 / 2 + 4 * 5 / 2 - 2;

        new int[5];
        new Parser();

        ioPlus.length;

		ioPlus.printHelloWorld(num, 3*5+num);

		return num;
	}
}