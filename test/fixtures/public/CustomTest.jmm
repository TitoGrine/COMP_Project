import static ioPlus.printHelloWorld(void) void;
import static ioPlus.print();
import ioPlus;
import ioPlus.plus(int, int, boolean);
import ioPlus.plus(int, int, int);
import Test;
import Stonk;

class CustomTest extends Test{
    int something;
    Stonk stonk;
    boolean truth;
    int a;

	public int stonk(int[] args, int nut) {
        int num;
        boolean star;

        args[5] + 2;
        num = 5;
        star = false;
        truth = !star;

        //if((num < 1 && num < 1)){
        if(5*3+4/num < args[2] && (2-3/45*34+5) < 4/4*1){
            num = 5;
        } else {
            num = 7;
        }

        while(num < 4){
            star = true;
            num = num/2 * 3+2;
        }

        if((2*3-4) < (num+4/2)){
            ioPlus.plus(2, 3, 2);
            (args[3] < 3) && star;
        } else {
            args[5];
            num = 7*3;
        }

        a = 8 / 2 + 4 * 5 / 2 - 2;

        new int[5];
        new Stonk();

        ioPlus.length;

		ioPlus.printHelloWorld();

		return num;
	}
}