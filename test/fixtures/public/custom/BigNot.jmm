import static io.println(int) void;

class BigNot {
    public static void main(String[] Args){
        int firstInt;
        boolean firstBoolean;
        BigNot ObjectInstance;
        firstInt= 0;
        firstBoolean = false;
        ObjectInstance = new BigNot();

        //True - prints 1
        if( ((!( 1 < firstInt && firstInt < 3 && !( (!firstBoolean) && firstInt < 0)) && !false)
                &&
                ObjectInstance.returnTrue()) 
            && 
                !ObjectInstance.returnFalse())
        {
            firstInt = 1;
            io.println(firstInt);
        }else{
            firstInt = 3;
            io.println(firstInt);
        }

        //True 10 times - prints 2 to 11
        while(!(10 < firstInt)){
            firstInt = firstInt + 1;
            io.println(firstInt);
        }
    }

    public boolean returnFalse(){
        return false;
    }
    public boolean returnTrue(){
        return true;
    }
}