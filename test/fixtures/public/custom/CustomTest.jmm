import static ioPlus.printHelloWorld(void) void;
import static ioPlus.print();
import ioPlus;
import ioPlus.plus(int, int, boolean);
import ioPlus.plus(int, int, int) int;
import Test;
import Test.test() int;
import Stonk;
import Stonk.stonk(boolean) boolean;

class CustomTest extends Test{
    int something;
    Stonk stonk;
    boolean truth;
    int a;

	public int stonk(int[] args, int nut) {
        int num;
        boolean star;
        CustomTest test;

        args[5] + 2;

        num = 2;

        test = new CustomTest();

        //if((num < 1 && num < 1)){
        if(5*3+4/num < this.test() && (2-3/45*34+5) < 4/4*1){
            num = 5;
            star = false;
        } else {
            num = 7;
            star = true;
        }

        while(num < 0){
            num = num/2 * 3+2;
        }

        if((2*3-4) < (num+4/2)){
            ioPlus.plus(2, 3, star);
            (args[3] < 3) && star;
        } else {
            args[5];
            num = 7*3;
        }

        a = 8 / 2 + num * 5 / 2 - 2;

        new int[5];

        this.methodOverload(new CustomTest());

		ioPlus.printHelloWorld();

		return num;
	}

	public int methodOverload(CustomTest i){
	    return 1;
	}
}