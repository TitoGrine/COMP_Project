import static io.println(int);
class JasminKeywords {
    int field;
    int putfield;
    int bipush;
    int ldc;
    int ireturn;
    boolean imul;
    boolean iconst_3;

	public static void main(String[] args) {
        JasminKeywords simple;

        simple = new JasminKeywords();

        io.println(simple.testKeywords());
	}

	public int testKeywords(){
	    ireturn = 0;
        field = 4;
        putfield = 5;
        bipush = 6;
        ldc = 7;
        imul = true;
        iconst_3 = true;

        if (imul && iconst_3) {
            ireturn = this.add(field, putfield, bipush, ldc);
        } else {
            ireturn = 0-1;
        }

        return ireturn;
	}

    public int add(int a, int b, int c, int d) {
        return a+b+c+d;
    }
}