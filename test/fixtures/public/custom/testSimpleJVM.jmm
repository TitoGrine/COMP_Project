import static io.println(int);
class SimpleJVM {

    int m;
	int[] jk;

	public static void main(String[] args) {
		int a;
		int b;
		boolean e;

		jk = new int[5];
		a = 10;
		b = jk[3];

		if(a < b){
        	m = b;
        } else{
            m = a;
        }

        e = 2 * (a + b) < m + a;

        if(!e){
            b = a * m;
        } else{
            a = b * m;
        }

        if(3 < a + b && e){
            m = jk.length * m;
        } else{
            m = jk.length / m;
        }

        jk[jk.length] = m;
	}
}