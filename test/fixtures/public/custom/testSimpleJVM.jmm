import static io.println(int);
import Tenso;

class SimpleJVM {

    int m;
	int[] jk;

	public static void main(String[] args) {
		int a;
		int b;
		boolean e;
		SimpleJVM object;

		object = new SimpleJVM();

		jk = new int[5];
		a = 10;
		b = jk[3];

		if(a < b){
        	m = b;
        } else{
            m = a;
        }

        e = 2 * (a + b) < m + a;

        if(!e){
            b = a * m;
        } else{
            a = b * m;
        }

        if(3 < a + b && e){
            m = jk.length * m;
        } else{
            m = jk.length / m;
        }

        jk[jk.length] = m;

        object = this.instance(object, new Tenso());
	}

    public SimpleJVM instance(SimpleJVM object, Tenso tenso){
        return new SimpleJVM();
    }
}